```
The following is a simple implementation of a print utilities for
the C programming language, inspired by other programming languages.

This includes:
  - print(format, ...);
  - println(format, ...);
  - printerr(format, ...);

To use this module in your codebase just include the header:

#include "print.h"
```
